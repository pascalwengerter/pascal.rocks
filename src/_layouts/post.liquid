---
layout: default
---

<div class="post-card">
    <img src="{{ page.img }}" class="mb-10">
    <h1>{{ page.title }}</h1>
    {{ content }}
    <a class="text-center block mt-12" href="/posts">Back to all posts</a>
</div>
